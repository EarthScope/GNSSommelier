id: COD
name: Center for Orbit Determination in Europe (AIUB)
website: https://www.aiub.unibe.ch/

servers:
  - id: code_https
    name: AIUB CODE Products HTTPS
    hostname: "https://www.aiub.unibe.ch/download"
    protocol: https
    auth_required: false
    description: >
      CODE product downloads at the University of Bern. Directory listings
      are provided by AIUB's S3-backed product browser; FTP is retired.

products:
  # ── Orbits ──────────────────────────────────────────────────────
  - id: code_orbit
    product_name: ORBIT
    server_id: code_https
    available: true
    description: CODE precise orbits
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: TTT, value: RAP}
      - {name: PPP, value: OPS}
      - {name: PPP, value: MGX}
      - {name: SMP, value: 05M}
      - {name: SMP, value: 15M}
    directory: {pattern: "CODE/"}

  # ── Clocks ──────────────────────────────────────────────────────
  - id: code_clock
    product_name: CLOCK
    server_id: code_https
    available: true
    description: CODE precise clocks
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: TTT, value: RAP}
      - {name: PPP, value: OPS}
      - {name: SMP, value: 30S}
      - {name: SMP, value: 05M}
    directory: {pattern: "CODE/"}

  # ── ERP ─────────────────────────────────────────────────────────
  - id: code_erp
    product_name: ERP
    server_id: code_https
    available: true
    description: Earth rotation parameters
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: TTT, value: RAP}
      - {name: PPP, value: OPS}
    directory: {pattern: "CODE/"}

  # ── Biases ──────────────────────────────────────────────────────
  - id: code_bias
    product_name: BIA
    server_id: code_https
    available: true
    description: Differential code biases (DCB/OSB)
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: TTT, value: RAP}
      - {name: PPP, value: OPS}
    directory: {pattern: "CODE/"}

  # ── Ionosphere ─────────────────────────────────────────────────
  - id: code_gim
    product_name: IONEX
    server_id: code_https
    available: true
    description: Global Ionosphere Maps (CODE is primary producer)
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: TTT, value: RAP}
      - {name: TTT, value: PRD}
      - {name: PPP, value: OPS}
      - {name: SMP, value: 01H}
      - {name: SMP, value: 02H}
    directory: {pattern: "CODE/"}

  # ── SINEX weekly solutions ──────────────────────────────────────
  - id: code_sinex
    product_name: SINEX
    server_id: code_https
    available: true
    description: CODE weekly station coordinate SINEX solutions
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: PPP, value: OPS}
    directory: {pattern: "CODE/{YYYY}/"}

  # ── Troposphere SINEX ──────────────────────────────────────────
  - id: code_trop
    product_name: TROP
    server_id: code_https
    available: true
    description: CODE troposphere SINEX (zenith total delay + horizontal gradients)
    parameters:
      - {name: AAA, value: COD}
      - {name: TTT, value: FIN}
      - {name: PPP, value: OPS}
      - {name: SMP, value: 01H}
    directory: {pattern: "CODE/{YYYY}/"}
